import { defineConfig } from 'astro/config';

export default defineConfig({
  // Replace 'username' with your GitHub username 
  // and 'repo-name' with your repository name
  site: 'https://jyyu99.github.io', 
});
